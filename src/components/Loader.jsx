import React from "react";
import "./Loader.css"; // Importă fișierul CSS pentru stiluri

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="loader">Loading...</div>
    </div>
  );
};

export default Loader;
